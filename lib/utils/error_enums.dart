enum PopularBooksError {
  networkError,
}

enum CategoriesError {
  networkError,
}

enum RecommendedBooksError {
  networkError,
}

enum WishListError {
  networkError,
}

enum BooksError {
  networkError,
}
